# 걸배이 연출용 사진

여기 넣은 파일을 `ANCHOR_PHOTOS`(`src/lib/config.ts`)에 puuid 별로 적으면
그 사람의 걸배이 연출에만 쓰인다. 적지 않은 사람은 MVP 사진으로 돌아간다.

MVP 사진은 한 단계 위 `public/players/` 에 있다. 섞이지 않게 분리해 둔다.
정사각형에 가깝고 얼굴이 가운데 오도록 자르면 원형 마스크에 잘 맞는다.
